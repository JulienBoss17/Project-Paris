<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style.css">
    <title>Mon Compte</title>
</head>
<body>
    <%- include('../Partials/navbar') %>

    <div class="container">
        <% if (success_msg) { %> <div class="flash-message success"><%= success_msg %></div> <% } %>
        <% if (error_msg || locals.error_msg2) { %> <div class="flash-message error"><%= error_msg || error_msg2 %></div> <% } %>

        <% if (user) { %>
            <div class="text-center">
                <h1>Bonjour, <%= user.prenom %> ! 👋</h1>
                <p class="text-muted">Ravi de vous revoir sur votre espace.</p>
            </div>

            <div class="stats" style="margin: 2rem 0;">
                <% if(user.role === "admin") { %>
                    <a href="/ajouter" class="btn">➕ Ajouter un prono</a>
                <% } %>
                <a href="/showuser/<%= user._id %>" class="btn" >⚙️ Voir mon Profil</a>
            </div>

            <% if(user.role === "admin") { %>
                <div class="annonce">
                    <h3>📢 Publier une annonce</h3>
                    <form action="/annonces/<%= user._id %>" method="POST">
                        <label for="titre">Titre</label>
                        <input type="text" id="titre" name="titre" placeholder="Titre accrocheur..." required>
                        
                        <label for="description">Message</label>
                        <textarea id="description" name="description" rows="3" placeholder="Contenu de l'annonce..." required></textarea>
                        
                        <button type="submit" class="btn" style="width: 100%;">Publier maintenant</button>
                    </form>
                </div>
            <% } %>

            <form action="/logout" method="POST" style="margin-top: 2rem;">
                <button type="submit" style="background: #fee2e2; color: #991b1b; width: 100%;">Se déconnecter</button>
            </form>

        <% } else { %>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 40px;">
                
                <div class="div" style="margin: 0; box-shadow: var(--shadow-sm);">
                    <h2 class="text-center">Connexion</h2>
                    <form action="/login" method="POST">
                        <label for="login_nom">Nom d'utilisateur</label>
                        <input type="text" id="login_nom" name="nom" required>
                        <label for="login_pass">Mot de passe</label>
                        <input type="password" id="login_pass" name="password" required>
                        <button type="submit" style="width: 100%;">Se connecter</button>
                    </form>
                </div>

                <div class="div" style="margin: 0; box-shadow: var(--shadow-sm);">
                    <h2 class="text-center">Créer un compte</h2>
                    <form action="/register" method="POST">
                        <label for="reg_nom">Nom</label>
                        <input type="text" id="reg_nom" name="nom" required>
                        <label for="reg_pre">Prénom</label>
                        <input type="text" id="reg_pre" name="prenom" required>
                        <label for="reg_pass">Mot de passe</label>
                        <input type="password" id="reg_pass" name="password" required>
                        <button type="submit" style="width: 100%; background: var(--secondary);">S'enregistrer</button>
                    </form>
                </div>

            </div>
        <% } %>
    </div>
    <%- include('../Partials/footer') %>
</body>
</html>