<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style.css">
    <title>Suppression | <%= user.nom %></title>
</head>
<body>
    <%- include('../Partials/navbar') %>

    <div class="container text-center" style="max-width: 500px; border: 2px solid #fee2e2;">
        <h1 style="color: var(--danger);">⚠️ Zone Dangereuse</h1>
        <p>Vous êtes sur le point de supprimer définitivement votre compte.</p>
        
        <div style="background: var(--bg-main); padding: 1rem; border-radius: var(--radius-md); margin: 1.5rem 0;">
            <p style="margin: 0;">Compte : <strong><%= user.nom %> <%= user.prenom %></strong></p>
        </div>

        <form action="/deleteuser/<%= user._id%>?_method=DELETE" method="POST">
            <button type="submit" class="btn" style="background: var(--danger); width: 100%;">Confirmer la suppression</button>
        </form>

        <a href="/compte" style="display: block; margin-top: 1.5rem; font-weight: 600; color: var(--text-muted);">Annuler et revenir</a>
    </div>

    <%- include('../Partials/footer') %>
</body>
</html>